datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model User {
  id        String   @id @unique @db.Uuid
  createdAt DateTime @default(now())
  email     String   @unique
  name      String?
  password  String
}